# Retired subset-pilot configurations

These files belong to the superseded 30/15/15 WebShop subset pilot. They are
retained only for provenance and must not be used for the full-WebShop policy
normalization experiment.

The active configurations are the `webshop_full_*.yaml` files in the parent
directory.
